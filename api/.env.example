PORT=4000
JWT_SECRET=replace-me-with-a-long-random-string
WEB_ORIGIN=http://localhost:3000

# Medusa (Express completes the Medusa cart after Wire payment succeeds)
MEDUSA_URL=http://localhost:9000
MEDUSA_PK=pk_your_publishable_key

# Wire Payment (https://wire.mn) — QPay + Mongolian bank apps.
# Leave WIRE_SECRET_KEY empty for MOCK mode (auto-succeeds after 5s, for dev/demo).
# sk_test_… = sandbox, sk_live_… = real QPay.
WIRE_SECRET_KEY=
WIRE_WEBHOOK_SECRET=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
